CAPI=2:

name: dune-daq:daphne:legacy-board-control-bridge:0.1.0
description: Legacy board-control compatibility bridge wrapping the stuff control block
provider:
  name: local

filesets:
  rtl:
    depend:
      - dune-daq:daphne:config-control:0.1.0
    files:
      - rtl/isolated/subsystems/control/legacy_board_control_bridge.vhd
    file_type: vhdlSource

targets:
  default:
    filesets:
      - rtl
