# Deprecated: bootstrap gap analysis

This document described the repository during its initial migration and is no
longer an accurate list of missing features. In particular, the repository now
contains PetaLinux image and deployment tooling that did not exist when the
analysis was written.

Use these current sources:

- `../README.md` for supported capabilities and boundaries;
- `project-overview.md` for architecture and current priorities;
- `verification-status.md` for checked verification scope;
- `kr260-petalinux-build-guide.md` for image generation and deployment.

The original text remains available in Git history.
