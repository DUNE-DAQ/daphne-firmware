# Deprecated: WSL agent summary

This was a temporary Vivado 2024.1 troubleshooting handoff. It is not a
supported build guide and does not represent the current 2026.1 flow.

Use `wsl-windows-vivado.md` for WSL operation or `remote-vivado.md` for a
remote Linux build host. Use `build-manual.md` for the complete build path.
