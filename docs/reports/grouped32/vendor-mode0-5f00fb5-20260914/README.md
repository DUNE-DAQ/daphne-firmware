# AMD XPM mode 0 replay for `5f00fb5`

Vivado 2026.1 XSIM passed nine reset, serializer, and continuation contract
snapshots plus grouped32 mode 0 using AMD's installed XPM memory, CDC, and
FIFO models at 700 ps phase offset. The packet oracle checked **3,392 grouped
and 3,392 reference packets**, all byte-identical, including **3,473,408
samples** and **81,408 descriptor words**. Exact tool commands, tool/vendor
hashes, analyzed source hashes, case metrics, and the packet CSV SHA-256 are
in `summary.json`.

The 19 source files shared with the local five-mode replay have identical
SHA-256 hashes. The summary's `source_status` names only two `.core` manifests
regenerated by the concurrent Vivado packaging preflight; no analyzed RTL
source differed from the pinned `5f00fb5` revision. This simulation does not
replace placed/routed timing or hardware testing.
